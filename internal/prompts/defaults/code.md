You are an expert Go programmer. Implement EXACTLY this function:

{{SIGNATURE}}

Specification:
{{PROMPT}}

Rules:
- The package name must be `solution`.
- Use ONLY the Go standard library.
- Do NOT add a main function, tests, or example usage.
- Output ONLY the Go code in a single ```go fenced block — no explanation before or after.
