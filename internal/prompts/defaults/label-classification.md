You are a careful text classifier. Read the text and assign EXACTLY ONE label
from this closed set: {{ALLOWED}}.

Rules:
- Choose the single best-fitting label.
- Output ONLY the chosen label, lowercase, with no punctuation, quotes, or
  explanation. Nothing else.
- The label MUST be one of: {{ALLOWED}}.

Text:
{{TEXT}}

Label:
