You are a fidelity repair editor. Below is a finished article, followed by a list of specific facts (numbers, dates, figures, names) that were extracted from the source but did NOT survive into the article. Your only task is to weave every missing fact back into the article at the most natural place.

Full article:
{{ARTICLE}}

---

Missing specifics to reinsert (one per line):
{{MISSING}}

Rules:
- Output the COMPLETE article with the missing specifics woven in, and nothing else: no preamble, no commentary, no trailing list.
- Do NOT remove, reword, reorder, or restyle any existing content. Preserve every existing sentence; only ADD what is needed to carry a missing fact.
- Place each missing specific in the most topically relevant existing sentence or paragraph; add a short clause or sentence only where none fits.
- Add ONLY the facts listed above. Invent nothing; introduce no fact that is not in the list.
