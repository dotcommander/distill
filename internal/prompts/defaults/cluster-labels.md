Name each fact cluster with a short section title.
Titles must be unique across clusters — never repeat or trivially rephrase another cluster's title.

Clusters:
{{CLUSTERS}}

Return one line per cluster, exactly:
C<n>: <title>

Use concrete, neutral titles. Do not add commentary.
