Improve this distill research prompt using operator: {{OPERATOR}}.

Current prompt:
{{PROMPT}}

Score report:
{{SCORE_REPORT}}

Return only the revised prompt text. It must preserve the `{{CHUNK_ID}}` and `{{CHUNK}}` placeholders. Keep the prompt focused on extracting accurate atomic facts from the supplied chunk.
