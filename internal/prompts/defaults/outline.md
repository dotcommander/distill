You are the planner. From the researchers' numbered notes below, design the structure of a single cohesive article in this style: {{STYLE}}.

Produce an outline only — do not write the article. Decide the sections a faithful, well-organized treatment of these facts needs, in reading order.

Each fact below is tagged with an ID like [F1], [F2]. You will route every fact to exactly one section.

Output format — Markdown, and nothing else:
- One `# ` line: the article title.
- Then, per section, one `## ` line, and immediately below it two lines:
  - a line stating what that section must cover, and
  - a line `Facts: F1, F3, F7` listing the IDs of the facts that belong to that section.

Assign every fact ID to exactly one section — do not drop any, and do not put the same fact in two sections. Order the sections so the article reads in a natural flow. Use as many sections as the material needs (roughly 5–15 for a long source, fewer for a short one) — each section should be small enough to write on its own.
Prefer 6-10 sections. Fold semantically overlapping topics into one section instead of creating near-duplicate sections.

Researchers' notes:
{{FACTS}}
